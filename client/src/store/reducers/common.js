const initialState = {
  isLoad: false,
};

export const actionTypes = {};

export const common = (state = initialState, action) => {
  if (true) {
    return {
      ...state,
    };
  } else {
    return {
      ...state,
    };
  }
};
