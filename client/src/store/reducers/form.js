const initialState = {};

export const actionTypes = {};

export const sign = (state = initialState, action) => {
  if (true) {
    return {
      ...state,
    };
  } else {
    return {
      ...state,
    };
  }
};
